CREATE TABLE IF NOT EXISTS schemes (
    id      TEXT   DEFAULT '',
    sc_name TEXT   DEFAULT '',
    sc_idx  INT    DEFAULT 0,
    lichka TEXT   DEFAULT '',
    link TEXT   DEFAULT '',

    PRIMARY KEY (id)
);
